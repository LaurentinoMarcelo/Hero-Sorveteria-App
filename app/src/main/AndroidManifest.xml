<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.herosorveteria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HeroSorveteria">
        <activity android:name=".cadastro.CadastroProdutoActivity"></activity>
        <activity android:name=".adapter.AdapterListaProdutos" />
        <activity android:name=".menu.ListadeClientesActivity" />
        <activity android:name=".menu.ProdutoActivity" />
        <activity android:name=".menu.ReceitaActivity" />
        <activity android:name=".Activity.LoginActivity" />
        <activity android:name=".menu.DespesasActivity" />
        <activity android:name=".cadastro.CadastroUsuarioActivity" />
        <activity android:name=".menu.HistoricoDeVendasActivity" />
        <activity android:name=".Activity.MenuActivity" />
        <activity android:name=".Activity.IniciarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>