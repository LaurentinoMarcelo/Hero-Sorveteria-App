<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.herosorveteria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HeroSorveteria">
        <activity android:name=".cadastro.CadastroClienteActivity"></activity>
        <activity android:name=".menu.ListaClientesActivity" />
        <activity android:name=".menu.ListaFornecedorActivity" />
        <activity android:name=".cadastro.CadastroFornecedorActivity" />
        <activity android:name=".cadastro.CadastroProdutoActivity" />
        <activity android:name=".adapter.AdapterListaProdutos" />
        <activity android:name=".menu.ListaProdutoActivity" />
        <activity android:name=".cadastro.CadastroReceitaActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".cadastro.CadastroDespesasActivity" />
        <activity android:name=".cadastro.CadastroUsuarioActivity" />
        <activity android:name=".menu.HistoricoDeVendasActivity" />
        <activity android:name=".activity.MenuActivity" />
        <activity android:name=".activity.IniciarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>